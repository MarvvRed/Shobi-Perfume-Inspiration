#!/usr/bin/env node
const fs=require('fs');
const path=require('path');
const {chromium}=require('playwright');
const ROOT=process.cwd();
const CDP_URL=process.env.SHOBI_CDP_URL||'http://127.0.0.1:9222';
const OUT=process.env.SHOBI_CATCHER_OUT||path.join(ROOT,'tools','fragrantica-main-notes','results','playwright-41-100');
const LOCKS=['Shobi Master Database/bestseller-top100-source-lock.csv','Shobi Master Database/bestseller-top100-source-lock-pass2.csv'];
const sleep=ms=>new Promise(r=>setTimeout(r,ms));
const clean=s=>String(s||'').replace(/\s+/g,' ').trim();
const slug=s=>String(s||'').replace(/[^a-z0-9]+/gi,'-').replace(/^-|-$/g,'').toLowerCase();
function read(p){return fs.readFileSync(path.join(ROOT,p),'utf8').replace(/^\uFEFF/,'');}
function csv(line){const a=[];let s='',q=false;for(let i=0;i<line.length;i++){const c=line[i];if(c==='"'){if(q&&line[i+1]==='"'){s+='"';i++;}else q=!q;}else if(c===','&&!q){a.push(s);s='';}else s+=c;}a.push(s);return a;}
function targets(){
  const byRank=new Map();
  for(const rel of LOCKS){
    const lines=read(rel).split(/\r?\n/).filter(Boolean); const h=csv(lines.shift());
    const ir=h.indexOf('rank'),ic=h.indexOf('shobi_code'),iu=h.indexOf('primary_source');
    if(ir<0||ic<0||iu<0) throw new Error(`BAD_LOCK_HEADER ${rel}`);
    for(const line of lines){const r=csv(line),rank=Number(r[ir]);if(rank<41||rank>100)continue;const code=clean(r[ic]),url=clean(r[iu]);if(!code||!/^https:\/\/(?:www\.)?fragrantica\.(?:com|it)\//i.test(url))continue;byRank.set(rank,{rank,code,url,url_source:rel});}
  }
  const out=[];for(let rank=41;rank<=100;rank++){const t=byRank.get(rank);if(!t)throw new Error(`SOURCE_LOCK_MISSING_RANK_${rank}`);out.push(t);}
  return out;
}
async function findVoteControl(page){const all=page.locator('button,a,[role="button"],span,div');for(let i=0,n=await all.count();i<n;i++){const e=all.nth(i),txt=clean(await e.innerText().catch(()=>'')),aria=clean(await e.getAttribute('aria-label').catch(()=>'')),title=clean(await e.getAttribute('title').catch(()=>''));if(/^(show|hide)\s+votes$/i.test(txt)||/(show|hide)\s+votes/i.test(aria)||/(show|hide)\s+votes/i.test(title))return e;}return null;}
async function collect(page){let ctl=null,end=Date.now()+30000;while(Date.now()<end&&!ctl){ctl=await findVoteControl(page);if(!ctl)await sleep(1000);}if(!ctl)throw new Error('SHOW_VOTES_NOT_FOUND');const label=clean(await ctl.innerText().catch(()=>''));if(!/^Hide\s+votes$/i.test(label)){await ctl.scrollIntoViewIfNeeded().catch(()=>{});await ctl.click({timeout:10000});await sleep(1200);}for(let k=0;k<10;k++){const notes=await page.evaluate(()=>{const c=s=>String(s||'').replace(/\s+/g,' ').trim();const idOf=h=>{const m=String(h||'').match(/\/notes\/[^/]*?(\d+)(?:\.html)?(?:[?#]|$)/i)||String(h||'').match(/id=(\d+)/i);return m?Number(m[1]):null;};const best=new Map();for(const a of document.querySelectorAll('a[href*="/notes/"]')){const raw=c(a.textContent)||c(a.querySelector('img[alt]')?.alt)||c(a.getAttribute('title'));const m=raw.match(/^\s*([0-9][0-9.,\s]*)\s*([^0-9].*?)\s*$/);if(!m)continue;const votes=Number(m[1].replace(/[^0-9]/g,'')),note=c(m[2]);if(!Number.isFinite(votes)||votes<=0||!note||note.length>80)continue;const id=idOf(a.getAttribute('href')),key=id!=null?`id:${id}`:`name:${note.toLowerCase()}`,prev=best.get(key);if(!prev||votes>prev.votes)best.set(key,{note,sastojak_id:id,votes});}return[...best.values()].sort((a,b)=>b.votes-a.votes||a.note.localeCompare(b.note));});if(notes.length)return notes;await new Promise(r=>setTimeout(r,750));}throw new Error('VOTED_NOTES_NOT_PARSED');}
async function capture(page,t){console.log(`\n=== #${t.rank} ${t.code} ===\nURL ${t.url}`);await page.goto(t.url,{waitUntil:'domcontentloaded',timeout:60000});await sleep(1800);const ranked=await collect(page),top5=ranked.slice(0,5).map((n,i)=>({rank:i+1,...n}));const idm=page.url().match(/-(\d+)\.html(?:[?#]|$)/i);const p={schema_version:1,source:'playwright-cdp-real-edge',captured_at:new Date().toISOString(),rank:t.rank,shobi_code:t.code,fragrantica_id:idm?Number(idm[1]):null,url:page.url(),url_source:t.url_source,total_voted_notes:ranked.length,saved_note_count:top5.length,notes:top5};fs.writeFileSync(path.join(OUT,`${String(t.rank).padStart(3,'0')}-${slug(t.code)}.json`),JSON.stringify(p,null,2)+'\n');for(const n of top5)console.log(`#${n.rank} ${n.note} votes=${n.votes} sastojak_id=${n.sastojak_id??''}`);return p;}
async function main(){fs.rmSync(OUT,{recursive:true,force:true});fs.mkdirSync(OUT,{recursive:true});const ts=targets();fs.writeFileSync(path.join(OUT,'canonical-targets-41-100.json'),JSON.stringify(ts,null,2)+'\n');console.log(`PREFLIGHT_OK targets=${ts.length} urls=${ts.length}`);for(const t of ts)console.log(`RESOLVE #${t.rank} ${t.code}: ${t.url}`);const browser=await chromium.connectOverCDP(CDP_URL,{timeout:15000});const ctx=browser.contexts()[0];if(!ctx)throw new Error('CDP_NO_BROWSER_CONTEXT');const pages=ctx.pages(),page=pages.find(p=>/fragrantica\.(com|it)/i.test(p.url()))||pages[0]||await ctx.newPage();const results=[],failures=[];for(const t of ts){let ok=false;for(let a=1;a<=2&&!ok;a++){try{results.push(await capture(page,t));ok=true;}catch(e){console.error(`ATTEMPT_FAIL #${t.rank} attempt=${a} ${e.message}`);if(a<2)await sleep(5000);else failures.push({rank:t.rank,code:t.code,url:t.url,error:e.message});}}await sleep(2500);}const sum={schema_version:1,captured_at:new Date().toISOString(),range:[41,100],success_count:results.length,failure_count:failures.length,results,failures};fs.writeFileSync(path.join(OUT,'batch-41-100.json'),JSON.stringify(sum,null,2)+'\n');console.log(`BATCH_SUMMARY success=${results.length} failures=${failures.length}`);await browser.close().catch(()=>{});if(failures.length)process.exit(2);}
main().catch(e=>{try{fs.mkdirSync(OUT,{recursive:true});fs.writeFileSync(path.join(OUT,'fatal-error.txt'),String(e?.stack||e)+'\n');}catch{}console.error('BATCH_FATAL',e?.stack||e);process.exit(1);});
