name: Legacy Sync English Shobi Catalog

# LEGACY WORKFLOW.
# The official catalog is now maintained by sync-shobi-master-official.yml
# inside `Shobi Master Database/`. This workflow is kept manual-only so it
# cannot overwrite or compete with the official Master pipeline.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  legacy-info:
    runs-on: ubuntu-latest
    steps:
      - name: Legacy workflow disabled
        run: |
          echo "This legacy sync is intentionally manual-only."
          echo "Use .github/workflows/sync-shobi-master-official.yml for the official Shobi Master Database."
